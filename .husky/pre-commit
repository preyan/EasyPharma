#!/usr/bin/env sh
echo "Running Quality Checks (Lint & Test)..."

# Run only against affected projects (not the entire monorepo)
pnpm nx affected -t lint --base=HEAD~1
pnpm nx affected -t test --base=HEAD~1
